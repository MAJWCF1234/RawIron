#pragma once

namespace ri::games::liminal {

/// Call once from the editor host so manifest-driven `editorPreviewScene` can resolve this game.
void RegisterLiminalHallEditorPreview();

} // namespace ri::games::liminal
